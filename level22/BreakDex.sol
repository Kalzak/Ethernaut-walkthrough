pragma solidity ^0.8.0;

contract BreakDex {
    function balanceOf(address caller) public view returns (uint256) {
        return 1;
    }
    
    function transferFrom(address from, address to, uint256 amount) public returns(bool){
        return true;
    }
}
